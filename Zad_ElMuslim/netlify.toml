# إعدادات النشر على Netlify
[build]
  command = "npm run build"
  publish = "dist"

# منع ظهور 404 عند التحديث (Refresh) على أي مسار - تطبيق SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
